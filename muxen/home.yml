# /home/afallows/.config/tmuxinator/home.yml

name: home
root: ~/laptop

windows:
  - dotfiles:
      layout: even-horizontal
      panes:
        - dotfiles
        - muxen
  - crawl: crawl
  - script:
      layout: even-horizontal
      panes:
        - nv -o linux Aptfile
        - git status

