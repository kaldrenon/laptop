# /home/afallows/.config/tmuxinator/trin.yml

name: trinity
root: ~/code/trinity/

windows:
  - code: nv Program.cs
  - server:
      layout: main-vertical
      panes:
        - dnbr
        - npx vite
        - cd ../trinity-test && dotnet test
  - shell: git status
  - tests: cd ../trinity-test/ && nv
